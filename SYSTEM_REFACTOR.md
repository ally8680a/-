# Mode1–6 交易系统重构蓝图（Script01 / Script02）

> 用途：指导 Cursor 进行系统级重构  
> 原则：不写交易代码，只写结构、职责、数据流、裁决边界  
> 目标：合并强势股系统 + Mode 系统 + 盘中大单系统，减少重复日志

---

## 一、系统总设计原则（必须遵守）

1. **系统只保留两个核心脚本**
   - `Script01`：盘后 / 盘前【个股筛选 & 模式裁决】
   - `Script02`：盘中【监控 & 买点建议】

2. **禁止并行选股系统**
   - 强势股 N 字弱转强不再独立存在
   - 必须合并进 `Script01` 的 Mode1 体系

3. **Script02 不再判断“是什么股”**
   - 所有模式、结构、资格判定只允许在 Script01
   - Script02 只负责验证、监控、给建议

4. **板块 ≠ 买点**
   - 板块只作为“背书 / 权重因子”
   - 严禁根据板块短周期涨跌反复改变个股结论

---

## 二、Script01（个股筛选 & 模式裁决）重构说明

### 2.1 最终 Mode 体系（写死）

#### 观察态（禁止交易）
- `Mode1_OBS`  
  普通弱转强结构，纯观察
- `Mode1_STRONG`  
  强势股 N 字弱转强（高质量 Mode1）
- `Mode6_PRE`  
  启动预确认（原 Mode4，观察态）

#### 交易态（允许进入 Script02）
- `Mode2`：底部解放
- `Mode3`：箱体震荡 / 波段
- `Mode4`：**龙头二波低吸（原 Mode7-A）**
- `Mode5`：弱势大盘强势股
- `Mode6`：启动确认 / 6C

❌ 禁止：
- Mode4 作为“分歧转一致”独立存在  
- 强势股系统单独输出  
- 同一股票同时多个 Mode

---

### 2.2 强势股 N 字系统的融合方式（核心）

强势股 N 字弱转强系统 **不再输出独立日志**，  
它在 Script01 中的作用是：

> **作为 Mode1 的“质量增强器”**

#### 融合规则（结构层）

- 命中强势股 N 字规则：
  ```text
  mode = Mode1
  mode_quality = STRONG
  structure_tag = N_WEAK_STRONG
  retrace_type = 缩量 / 换手 / 下杀
  ```

* 普通弱转强：

  ```text
  mode = Mode1
  mode_quality = NORMAL
  ```

📌 注意

* Mode 不变（仍是 Mode1）
* 强势与否是 **质量标签，不是新模式**

---

### 2.3 Script01 输出字段规范（Script02 的唯一事实来源）

每只股票必须输出：

```text
code
name
mode                  # Mode1 / Mode2 / Mode3 / Mode4 / Mode5 / Mode6
mode_quality          # STRONG / NORMAL / NONE
structure_tag         # N_WEAK_STRONG / BOX / START / etc
block_id              # 行业 / 板块 ID
block_strength_rank   # 板块内涨幅排名（百分位）
is_block_leader       # 是否板块内领涨（True / False）
leader_pool_id        # 龙头池ID / 名称（可为空）
leader_pool_rank      # 龙头池内排名（百分位，可为空）
allow_trade           # True / False
allow_intraday_check  # True / False
```

❌ Script02 禁止重新计算这些字段

---

## 三、Script02（盘中监控 & 买点建议）重构说明

### 3.1 Script02 的唯一职责

* 盘中监控
* 验证 Script01 给出的“可交易资格”
* 输出买点 / 风险 / 加减仓建议

❌ 禁止：

* 再判断 Mode
* 再判断是否强势股

---

### 3.2 盘中大单监控的融合方式

原“强势股盘中大单监控”逻辑并入 Script02，作为 **确认信号**：

```text
if mode_quality == STRONG:
    使用更宽松的大单确认阈值
else:
    使用标准阈值
```

📌 大单信号只影响：

* 建议强度
* 是否加仓
  不影响 Mode 本身

---

### 3.3 板块背书机制（解决板块乱跳问题）

#### 核心原则

* 板块只做“相对强度背书”
* 不追随半小时级别轮动

#### 板块评估时间点（写死）

* 10:00
* 11:30
* 14:00

#### 板块背书规则

```text
若：
- 个股涨幅 > 所属板块涨幅
- 且个股处于板块涨幅 Top X%
→ 视为板块内领涨 / 龙头
```

📌 解释：

* 板块跌，个股不跌 → 强背书
* 板块涨，个股跑输 → 无背书

板块只作为 **置信度加权因子**，不直接触发买点。

---

## 四、Script02 买点建议分级规则（新增）

### 4.1 建议等级（统一口径）

```text
OBSERVE        ：观察
WATCH          ：关注，等待确认
LOW_RISK_BUY   ：可低吸
ADD_POSITION   ：可加仓
RISK_ALERT     ：注意风险
EXIT           ：建议退出
```

---

### 4.2 建议生成逻辑（结构层）

#### 低吸 / 买入建议

```text
allow_trade == True
AND 结构未破坏
AND 出现盘中承接 / 大单确认
```

* 若 `mode_quality == STRONG`
  → 建议等级提升一档

* 若 `is_block_leader == True`
  → 置信度提升（但不单独触发）

---

### 4.3 风险 / 退出建议

```text
若：
- 跌破关键结构位
- 盘中出现连续放量下杀
- 与 Script01 结构结论冲突
→ 输出 RISK_ALERT / EXIT
```

---

## 五、最终系统验收标准

1. 每天只看两套日志

   * Script01：**今天值得关注哪些股票**
   * Script02：**这些股票现在能不能动**

2. 强势股不再是第三套系统

   * 成为 Mode1 的增强器

3. Mode 结构清晰、不打架

   * Mode1：结构观察
   * Mode4：龙头二波
   * Mode6：第一波启动

4. 板块不再“半小时翻脸”

   * 只看相对强度和领涨属性

---

## 系统宪法（一句话）

> **Script01 决定资格，Script02 决定行动；
> 结构在盘后完成，盘中只验证，不重判。**

---

# Cursor 重构补丁文档：Script02 盘中裁决顺序表（含 6C / 大单 / 板块）+ 6C 并入 02 改造清单

> 目标：盘中只看新版 Script02；6C 不再独立输出，不与 Mode 冲突；板块背书稳定不乱跳  
> 原则：不写交易代码，只写裁决顺序、字段口径、日志规范、需要改哪些模块

===============================================================================
0) 总宪法（必须写进注释/README）
===============================================================================
- Mode（模式/阶段）只能来自 Script01：Script02 禁止重判 Mode
- 6C 是“盘中事件信号”，只能加分/降级建议强度，禁止改变 Mode
- 盘中唯一 Action 输出口 = Script02（6C 不再单独打印“买/不买”）
- 板块用于背书（相对强度加权），不用于短周期追随轮动

===============================================================================
1) Script02 输入与事实源（写死）
===============================================================================
Script02 只读取：
A. Script01 输出（事实源）
- code, name
- mode (Mode1/2/3/4/5/6)
- mode_quality (STRONG/NORMAL)
- structure_tag
- allow_trade (True/False)
- allow_intraday_check (True/False)
- block_id
- block_strength_rank (百分位)
- is_block_leader (True/False)  # 来自01或02固定时点评估
- key_levels (可选：关键位，用于破坏判断)

B. 盘中行情/资金事实（实时）
- px_change_rate / close / high_px / low_px 等（按现有接口）
- 主动大单/资金承接代理信号（你已有的无tick代理即可）
- 指数/情绪（你已有的弱势大盘判断即可）
- 板块涨跌幅（用于相对强度）

禁止：
- Script02 再做 Mode1~6 的结构判定
- Script02 依据盘中形态“改 mode”

===============================================================================
2) Script02 盘中裁决顺序表（唯一合法顺序）
===============================================================================
对每只股票，按以下顺序生成“唯一行动建议 action_advice”：

--------------------------------------------------
Step 0：资格门禁（Gate）
--------------------------------------------------
IF allow_intraday_check == False:
    action_advice = "OBSERVE"
    reason = "观察态：不进入盘中裁决"
    STOP

IF allow_trade == False:
    action_advice = "WATCH"
    reason = "未获得交易资格：等待盘后/盘前升级"
    STOP
# 通过 Gate 后才允许进入后续裁决（可交易态）

--------------------------------------------------
Step 1：结构破坏检查（Fail-fast）
--------------------------------------------------
判断是否触发“结构破坏/硬风控”
- 跌破关键结构位（来自 Script01 的 key_levels 或 mode内默认关键位）
- 盘中出现连续放量下杀（你已有代理信号）
- 日内触发黑名单（如 ±7% 的风险锁定规则，沿用你现有口径）

IF 触发结构破坏:
    action_advice = "EXIT" 或 "RISK_ALERT"
    reason = "结构破坏/风控触发"
    STOP
# 结构破坏必须早退出，禁止被 6C/大单/板块背书“拉回去”

--------------------------------------------------
Step 2：盘中承接/资金确认（Core）
--------------------------------------------------
计算/读取盘中“承接强度”信号（你已有无tick代理）
输出：
- receive_score（0~100）
- big_order_confirm（True/False）
- sell_pressure（True/False）
- intraday_state（HOLDING/REPAIR/WEAK 等）

此阶段不输出最终建议，仅形成事实评分。

--------------------------------------------------
Step 3：6C 事件检测（Event，不改 Mode）
--------------------------------------------------
新增统一事件字段：
- is_6c_event_today: True/False
- 6c_strength: WEAK/MID/STRONG（可选）

规则：6C 只能用于“加分/提升建议等级”，禁止：
- 改 mode
- 单独打印“建议买/不买”
- 推翻 Step1 的结构破坏

并入原则：
IF is_6c_event_today == True:
    event_bonus += X（加分）
    add_reason("6C资金接管确认")

--------------------------------------------------
Step 4：板块背书（Stable Backing，不随半小时乱跳）
--------------------------------------------------
板块背书只在固定时点评估（写死 3 个时点）：
- 10:00
- 11:30
- 14:00
其余时间只沿用“最近一次背书状态”，不刷新结论。

背书判定口径（相对强度）：
- stock_return > block_return
- 且 stock 位于 block 涨幅 Top X%（或使用 is_block_leader=True）
=> block_backing = True

作用：
- 仅增加 confidence，不直接触发买点
IF block_backing == True:
    confidence_bonus += Y
    add_reason("板块背书：领涨/跑赢板块")

--------------------------------------------------
Step 5：综合打分 → 输出唯一行动建议（Action）
--------------------------------------------------
形成 final_score：
final_score = receive_score + event_bonus + confidence_bonus - risk_penalty

输出 action_advice（统一口径）：
- OBSERVE        ：观察（不进裁决或低信号）
- WATCH          ：关注等待（可交易态但未确认）
- LOW_RISK_BUY   ：可低吸（承接确认 + 风险可控）
- ADD_POSITION   ：可加仓（强承接 + 6C 或强背书）
- RISK_ALERT     ：注意风险（信号弱或有隐患）
- EXIT           ：建议退出（结构破坏/风控触发）

注意：
- 任何时候 Step1 触发 EXIT/RISK_ALERT，后续不得升级为 BUY
- 6C 只能提升建议等级 1 档（如 WATCH→BUY 或 BUY→ADD），不得跨两档

===============================================================================
3) 各 Mode 在 Script02 的“默认关键位 & 监控重点”（不重判结构，只做验证）
===============================================================================
- Mode2（底部解放）：
  重点：承接是否持续、是否回落吞没、是否破启动阳线关键位
- Mode3（箱体）：
  重点：箱底低吸守不守、箱顶站稳确认、箱体内不追涨
- Mode4（龙头二波低吸，原 Mode7-A）：
  重点：回踩不破第一波关键位/趋势位；盘中承接 + 非情绪高潮；允许低吸区间
- Mode5（弱市强股）：
  重点：指数弱时仍抗跌/逆势；若指数转强，优先给更高建议等级
- Mode6（启动确认/6C）：
  重点：回踩不破启动结构；6C 作为强确认事件；承接稳定优先

说明：
- 关键位建议由 Script01 输出（key_levels），Script02 只验证是否破坏
- 若 Script01 暂未输出 key_levels，先用 mode内默认关键位占位（后续补齐）

===============================================================================
4) 6C 并入 Script02：必须改的内容清单（工程级）
===============================================================================
目标：删除“独立 6C 盘中输出”，保留“6C 检测逻辑”作为事件信号，统一到 Script02 的建议系统。

--------------------------------------------------
4.1 需要保留的（逻辑保留）
--------------------------------------------------
- 6C 检测函数/逻辑本体（detect_6c_event / calc_6c_impulse 等）
- 相关输入字段（涨幅、量能、实体、承接代理等）
- 6C 历史字段（last_6c_age_days / is_6c_today 等）如 Script01 已写入则直接读取

--------------------------------------------------
4.2 需要删除/禁用的（输出层必须消失）
--------------------------------------------------
- 任何 “6C 独立日志板块/独立面板”
- 任何 “6C 单独给出买/不买/仓位” 的输出
- 任何基于 6C “改 mode / 改结构状态” 的逻辑
- 任何与 Script02 action_advice 并行的裁决结果（双裁决源）

--------------------------------------------------
4.3 需要新增/对齐的（字段与接口）
--------------------------------------------------
在 Script02 的每只票输出结构里新增：
- is_6c_event_today (bool)
- 6c_strength (enum: WEAK/MID/STRONG)  # 可选
- event_reasons (list)  # 用于解释：例如 ["6C资金接管确认"]

并在最终 action_advice 解释中体现（但不另起 6C 面板）：
示例：
【Mode6 | STRONG】承接良好｜6C确认｜板块领涨 → LOW_RISK_BUY

--------------------------------------------------
4.4 6C 触发对 action 的映射（写死）
--------------------------------------------------
建议映射（最多提升 1 档）：
- WATCH + 6C确认 → LOW_RISK_BUY
- LOW_RISK_BUY + 6C确认 → ADD_POSITION
- ADD_POSITION 维持（不再升级）

约束：
- 若 Step1 风控触发（RISK_ALERT/EXIT），6C 不得提升建议
- 6C 只在 “allow_trade=True” 的可交易态生效（观察态不生效）

===============================================================================
5) 板块背书稳定化：需要改的内容清单
===============================================================================
问题：板块轮动导致半小时升升降降，影响决策一致性。

改造要求：
1) 板块评估只在 10:00 / 11:30 / 14:00 三个时点更新
2) 其余时间沿用最近一次 block_backing 结果（缓存）
3) 背书口径只看相对强度（股票跑赢板块 + 板块内TopX%）

Script02 输出必须包含：
- block_return（最近一次评估值）
- stock_return（当前）
- is_block_leader（True/False）
- block_backing（True/False）

===============================================================================
6) 日志最终形态（你盘中只看这一份）
===============================================================================
每只票一行/一块，必须包含：
- 股票 + Mode（来自01）
- mode_quality（STRONG/NORMAL）
- 6C_event（是/否）
- block_backing（是/否，领涨/跑赢）
- action_advice（唯一建议）
- reasons（不超过3条，解释建议）

示例：
【603XXX | Mode6 | STRONG】承接=强｜6C=是｜板块背书=领涨 → LOW_RISK_BUY
【300YYY | Mode4 | NORMAL】承接=中｜6C=否｜板块背书=否 → WATCH
【600ZZZ | Mode3 | NORMAL】破箱底关键位｜风控触发 → EXIT

===============================================================================
7) 验收标准（你跑完立刻知道是否成功）
===============================================================================
- 盘中只看 Script02 即可：无任何 6C 独立面板/独立裁决输出
- 同一股票不会出现“两个地方给两种建议”
- 6C 的价值体现在建议等级提升，而不是旁白
- 板块背书不再半小时翻脸：固定时点评估 + 相对强度判定
- 你能一眼看出：Mode是什么、是否强势、是否6C、是否板块领涨、最终行动建议是什么
